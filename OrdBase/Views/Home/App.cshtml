@using OrdBase.ViewModels
@model AppViewModel



<main id="appMain">
    <section id="clientHeading">
        <h2></h2>
    </section>

    <group id="dynamicBody">
        <section id="categorySection">
            <h3>Categories</h3>
            <ul id="categoryList" class="DynamicList">
            @{
                var i = 0;
                foreach (var o in Model.Categories) 
                {
                    <li id="cat@(i)" class="ListItem CategoryListItem">@o.Name</li>
                    i++;
                }
            }
            </ul>
        </section>
        <section id="translationSection">
            <h3>Translations</h3>
            <ul id="translationList" class="DynamicList">
            @{
                var j = 0;
                foreach (var o in Model.Translations)
                {
                    <li id="trans@(j)" class="ListItem TranslationListItem">@o.Key - @o.Value - @o.Language.ShortName</li>
                    j++;
                }
            }
            </ul>
        </section>
    </group>
</main>
